version: '3.8'

services:
  backend:
    build:
      context: .      
      dockerfile: Dockerfile         
    ports:
      - "9000:9000"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge